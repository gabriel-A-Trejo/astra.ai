import dedent from "dedent";

export const improvementPrompt = dedent`
 I want you to improve the user prompt that is wrapped in \`user prompt\` tags.

          IMPORTANT: Only respond with the improved prompt and nothing else!

         user prompt:
        `;
